# Неделя 10 (4-8 мая 2026)

## Добавление backend-модуля оценок в электронный журнал

### 1. Цель недели

На этой неделе основной фокус был на расширении backend-части электронного журнала модулем оценок.

Ключевые задачи:
- добавить хранение контрольных точек по предметам
- добавить хранение оценок студентов
- реализовать backend API для преподавателя и студента
- добавить ограничения для балльно-рейтинговой системы
- связать оценки с существующими студентами, преподавателями, предметами и attendance-сессиями
- сохранить запуск проекта через `docker compose`

### 2. Миграция БД для оценок

Добавлена миграция:
- `migrations/20260507120000_add_grades.sql`

В миграции добавлены две основные таблицы:
- `grade_items` - контрольные точки по предмету
- `grades` - оценки студентов за контрольные точки

Фрагмент создания таблицы контрольных точек:

```sql
CREATE TABLE IF NOT EXISTS public.grade_items (
    item_id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    subject_id INTEGER NOT NULL REFERENCES public.subjects(subject_id) ON DELETE CASCADE,
    title VARCHAR(255) NOT NULL,
    max_score INTEGER NOT NULL DEFAULT 0 CONSTRAINT grade_items_positive_max_score CHECK (max_score > 0),
    item_type VARCHAR(50) NOT NULL DEFAULT 'current',
    deadline TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

Фрагмент создания таблицы оценок:

```sql
CREATE TABLE IF NOT EXISTS public.grades (
    grade_id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    student_id INTEGER NOT NULL REFERENCES public.students(student_id) ON DELETE CASCADE,
    item_id INTEGER NOT NULL REFERENCES public.grade_items(item_id) ON DELETE CASCADE,
    teacher_id INTEGER REFERENCES public.teachers(teacher_id) ON DELETE SET NULL,
    score INTEGER NOT NULL DEFAULT 0 CONSTRAINT grades_non_negative_score CHECK (score >= 0),
    session_id INTEGER REFERENCES public.attendance_sessions(session_id) ON DELETE SET NULL,
    comment TEXT,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    UNIQUE (student_id, item_id)
);
```

Практический эффект:
- у предмета появилась структура контрольных работ
- у студента появилась оценочная ведомость
- оценку можно привязать к преподавателю и занятию
- повторная оценка за одну работу не создает дубль

### 3. Ограничения БРС в базе данных

В миграции добавлены проверки на уровне PostgreSQL.

Первое ограничение: сумма максимальных баллов по одному предмету не может превышать 100.

```sql
CREATE OR REPLACE FUNCTION public.check_subject_score_limit()
RETURNS TRIGGER AS $$
DECLARE
    total_max INTEGER;
BEGIN
    SELECT COALESCE(SUM(max_score), 0)
    INTO total_max
    FROM public.grade_items
    WHERE subject_id = NEW.subject_id
      AND item_id IS DISTINCT FROM NEW.item_id;

    IF (total_max + NEW.max_score) > 100 THEN
        RAISE EXCEPTION 'Суммарный балл по предмету (БРС) не может превышать 100. Текущая сумма: %', total_max;
    END IF;

    RETURN NEW;
END;
$$ LANGUAGE plpgsql;
```

Второе ограничение: оценка студента не может быть больше максимума за контрольную точку.

```sql
CREATE OR REPLACE FUNCTION public.check_grade_score_limit()
RETURNS TRIGGER AS $$
DECLARE
    item_max INTEGER;
BEGIN
    SELECT max_score
    INTO item_max
    FROM public.grade_items
    WHERE item_id = NEW.item_id;

    IF NEW.score > item_max THEN
        RAISE EXCEPTION 'Оценка не может превышать максимум за контрольную точку (%)', item_max;
    END IF;

    RETURN NEW;
END;
$$ LANGUAGE plpgsql;
```

Практический эффект:
- некорректные данные блокируются на уровне БД
- backend не может случайно записать оценку выше допустимой
- БРС по предмету сохраняет предел в 100 баллов

### 4. Модели данных в Go

В `internal/db/models.go` добавлены модели:
- `GradeItem`
- `Grade`
- `StudentGradePoint`
- `PredictionStats`

Основные структуры:

```go
type GradeItem struct {
	ID        int32      `json:"item_id"`
	SubjectID int32      `json:"subject_id"`
	Title     string     `json:"title"`
	MaxScore  int32      `json:"max_score"`
	ItemType  string     `json:"item_type"`
	Deadline  *time.Time `json:"deadline,omitempty"`
	CreatedAt time.Time  `json:"created_at"`
}

type Grade struct {
	ID        int32     `json:"grade_id"`
	StudentID int32     `json:"student_id"`
	ItemID    int32     `json:"item_id"`
	TeacherID *int32    `json:"teacher_id,omitempty"`
	Score     int32     `json:"score"`
	SessionID *int32    `json:"session_id,omitempty"`
	Comment   *string   `json:"comment,omitempty"`
	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`
}
```

Практический эффект:
- backend получил типизированные структуры для работы с оценками
- JSON-ответы API сразу соответствуют ожидаемым полям внешних клиентов

### 5. Репозиторий оценок

Добавлен файл:
- `internal/db/grade_repository.go`

В репозитории реализованы методы:
- `CreateGradeItem`
- `GetGradeItemByID`
- `GetGradeItemsBySubject`
- `UpsertGrade`
- `GetStudentGradesBySubject`
- `GetSubjectStatsForPrediction`

Фрагмент создания контрольной точки:

```go
func (r *GradeRepository) CreateGradeItem(ctx context.Context, item GradeItem) (GradeItem, error) {
	item.Title = strings.TrimSpace(item.Title)
	item.ItemType = strings.TrimSpace(item.ItemType)
	if item.SubjectID <= 0 {
		return GradeItem{}, fmt.Errorf("subject id is required")
	}
	if item.Title == "" {
		return GradeItem{}, fmt.Errorf("grade item title is required")
	}
	if item.MaxScore <= 0 {
		return GradeItem{}, fmt.Errorf("max score must be positive")
	}
	if item.ItemType == "" {
		item.ItemType = "current"
	}

	var out GradeItem
	var deadline sql.NullTime
	err := r.pool.QueryRow(
		ctx,
		`INSERT INTO grade_items (subject_id, title, max_score, item_type, deadline)
		 VALUES ($1, $2, $3, $4, $5)
		 RETURNING item_id, subject_id, title, max_score, item_type, deadline, created_at`,
		item.SubjectID,
		item.Title,
		item.MaxScore,
		item.ItemType,
		item.Deadline,
	).Scan(&out.ID, &out.SubjectID, &out.Title, &out.MaxScore, &out.ItemType, &deadline, &out.CreatedAt)
	if err != nil {
		return GradeItem{}, fmt.Errorf("create grade item: %w", err)
	}
	if deadline.Valid {
		out.Deadline = &deadline.Time
	}

	return out, nil
}
```

Фрагмент выставления или обновления оценки:

```go
func (r *GradeRepository) UpsertGrade(ctx context.Context, grade Grade) (Grade, error) {
	if grade.StudentID <= 0 {
		return Grade{}, fmt.Errorf("student id is required")
	}
	if grade.ItemID <= 0 {
		return Grade{}, fmt.Errorf("grade item id is required")
	}
	if grade.Score < 0 {
		return Grade{}, fmt.Errorf("score must be non-negative")
	}

	var out Grade
	err := r.pool.QueryRow(
		ctx,
		`INSERT INTO grades (student_id, item_id, teacher_id, score, session_id, comment)
		 VALUES ($1, $2, $3, $4, $5, $6)
		 ON CONFLICT (student_id, item_id)
		 DO UPDATE SET
		     score = EXCLUDED.score,
		     teacher_id = EXCLUDED.teacher_id,
		     session_id = EXCLUDED.session_id,
		     comment = EXCLUDED.comment
		 RETURNING grade_id, student_id, item_id, teacher_id, score, session_id, comment, created_at, updated_at`,
		grade.StudentID,
		grade.ItemID,
		grade.TeacherID,
		grade.Score,
		grade.SessionID,
		grade.Comment,
	).Scan(
		&out.ID,
		&out.StudentID,
		&out.ItemID,
		&out.TeacherID,
		&out.Score,
		&out.SessionID,
		&out.Comment,
		&out.CreatedAt,
		&out.UpdatedAt,
	)
	if err != nil {
		return Grade{}, fmt.Errorf("upsert grade: %w", err)
	}

	return out, nil
}
```

Практический эффект:
- вся работа с таблицами оценок вынесена в отдельный репозиторий
- сервисный слой не содержит SQL-запросы напрямую
- обновление оценки реализовано атомарно через `ON CONFLICT`

### 6. Подключение репозитория в Store

В `internal/db/store.go` добавлено поле:

```go
type Store struct {
	pool *pgxpool.Pool

	Users           *UserRepository
	Lecterns        *LecternRepository
	ControlTypes    *ControlTypeRepository
	Subjects        *SubjectRepository
	SubjectMetrics  *SubjectMetricRepository
	SemesterLoads   *SemesterLoadRepository
	SubjectControls *SubjectControlRepository
	Teachers        *TeacherRepository
	Groups          *GroupRepository
	Students        *StudentRepository
	Attendance      *AttendanceRepository
	Grades          *GradeRepository
}
```

И инициализация:

```go
Grades: NewGradeRepository(pool),
```

Практический эффект:
- модуль оценок подключен к общей инфраструктуре БД
- сервис может обращаться к оценкам через `s.store.Grades`

### 7. Service-логика для оценок

Добавлен файл:
- `internal/app/grades.go`

В нем реализована бизнес-логика:
- проверка роли преподавателя
- поиск профиля преподавателя
- проверка существования предмета
- проверка доступа преподавателя к предмету
- проверка существования студента
- проверка максимального балла перед сохранением оценки
- получение ведомости студента

Фрагмент проверки доступа преподавателя к предмету:

```go
func (s *Service) teacherCanManageSubject(ctx context.Context, teacherID, subjectID int32) (bool, error) {
	var allowed bool
	err := s.store.Pool().QueryRow(
		ctx,
		`SELECT EXISTS (
		     SELECT 1
		     FROM schedules
		     WHERE teacher_id = $1 AND subject_id = $2
		 ) OR EXISTS (
		     SELECT 1
		     FROM teachers t
		     JOIN users u ON u.id = t.user_id
		     WHERE t.teacher_id = $1
		       AND u.login = 'teacher_test'
		 )`,
		teacherID,
		subjectID,
	).Scan(&allowed)
	if err != nil {
		return false, fmt.Errorf("check teacher subject access: %w", err)
	}
	return allowed, nil
}
```

Фрагмент сохранения оценки:

```go
func (s *Service) upsertGradeByTeacher(sessionToken string, data GradeUpsertData) Response {
	teacherUser, err := s.userBySessionToken(sessionToken)
	if err != nil {
		return Response{OK: false, Error: err.Error()}
	}
	if teacherUser.Role != "teacher" {
		return Response{OK: false, Error: "forbidden: teacher role required"}
	}

	teacherProfile, err := s.teacherProfileByUser(teacherUser)
	if err != nil {
		return Response{OK: false, Error: err.Error()}
	}

	ctx, cancel := s.dbContext()
	defer cancel()

	item, found, err := s.store.Grades.GetGradeItemByID(ctx, data.ItemID)
	if err != nil {
		return Response{OK: false, Error: "failed to load grade item"}
	}
	if !found {
		return Response{OK: false, Error: "grade item not found"}
	}
	if resp := s.ensureTeacherSubjectAccess(ctx, teacherProfile.ID, item.SubjectID); !resp.OK {
		return resp
	}

	if data.Score < 0 {
		return Response{OK: false, Error: "score must be non-negative"}
	}
	if data.Score > item.MaxScore {
		return Response{OK: false, Error: "score exceeds grade item max_score"}
	}

	teacherID := teacherProfile.ID
	grade, err := s.store.Grades.UpsertGrade(ctx, db.Grade{
		StudentID: data.StudentID,
		ItemID:    data.ItemID,
		TeacherID: &teacherID,
		Score:     data.Score,
		SessionID: data.SessionID,
		Comment:   data.Comment,
	})
	if err != nil {
		return Response{OK: false, Error: "failed to save grade"}
	}

	return Response{OK: true, Result: grade}
}
```

Практический эффект:
- преподаватель не может выставлять оценки по несуществующим работам
- студент должен существовать в БД
- проверка прав выполняется до записи оценки
- тестовый преподаватель `teacher_test` может использовать любые существующие предметы для проверки сценариев

### 8. Получение оценок студентом

Студент получает оценки только для собственного профиля.

Фрагмент:

```go
func (s *Service) gradesBySubjectForStudent(sessionToken string, data GradeSubjectData) Response {
	studentUser, err := s.userBySessionToken(sessionToken)
	if err != nil {
		return Response{OK: false, Error: err.Error()}
	}
	if studentUser.Role != "student" {
		return Response{OK: false, Error: "forbidden: student role required"}
	}

	studentProfile, err := s.studentProfileByUser(studentUser)
	if err != nil {
		return Response{OK: false, Error: err.Error()}
	}

	return s.studentGradesResult(studentProfile.ID, data.SubjectID)
}
```

Практический эффект:
- студент не передает `student_id` в запросе
- backend определяет студента по JWT-токену
- студент не может запросить оценки другого пользователя через этот endpoint

### 9. HTTP API для оценок

В `internal/httpserver/server.go` добавлены endpoints:

```go
fiberApp.Post("/api/teacher/grades/items", s.teacherCreateGradeItemHandler)
fiberApp.Post("/api/teacher/grades/items/list", s.teacherGradeItemsBySubjectHandler)
fiberApp.Post("/api/teacher/grades", s.teacherUpsertGradeHandler)
fiberApp.Post("/api/teacher/grades/student", s.teacherStudentGradesBySubjectHandler)
fiberApp.Post("/api/student/grades", s.studentGradesBySubjectHandler)
```

Назначение ручек:
- `POST /api/teacher/grades/items` - создать контрольную точку
- `POST /api/teacher/grades/items/list` - получить контрольные точки по предмету
- `POST /api/teacher/grades` - поставить или обновить оценку
- `POST /api/teacher/grades/student` - посмотреть ведомость студента
- `POST /api/student/grades` - студент смотрит свои оценки

Фрагмент общего HTTP-обработчика для grade-действий:

```go
func (s *Server) gradeActionHandler(c *fiber.Ctx, requestID, action string, body any) error {
	token := c.Get("Authorization")
	if token == "" {
		return c.Status(fiber.StatusUnauthorized).JSON(app.Response{OK: false, Error: "missing Authorization header"})
	}

	if err := c.BodyParser(body); err != nil {
		return c.Status(fiber.StatusBadRequest).JSON(app.Response{OK: false, Error: "Error parsing body"})
	}

	data, err := json.Marshal(body)
	if err != nil {
		return c.Status(fiber.StatusInternalServerError).JSON(app.Response{OK: false, Error: "Error marshalling request"})
	}

	req := app.Request{ID: requestID, Action: action, Token: token, Data: data}
	raw, err := json.Marshal(req)
	if err != nil {
		return c.Status(fiber.StatusInternalServerError).JSON(app.Response{OK: false, Error: "Error marshalling envelope"})
	}

	resp, err := s.svc.DispatchRequest(string(raw), s.requestTimeout)
	if err != nil {
		return c.Status(fiber.StatusServiceUnavailable).JSON(app.Response{OK: false, Error: err.Error()})
	}
	if !resp.OK {
		return c.Status(app.GradeHTTPStatus(resp)).JSON(resp)
	}

	return c.JSON(resp)
}
```

Практический эффект:
- HTTP-слой использует существующий request/action механизм
- обработка ошибок унифицирована
- endpoints оценок встроены в текущую архитектуру сервиса

### 10. Интеграция с посещаемостью

Оценка может быть связана с attendance-сессией через поле:

```sql
session_id INTEGER REFERENCES public.attendance_sessions(session_id) ON DELETE SET NULL
```

В сервисе добавлена проверка:
- если `session_id` передан, сессия должна существовать
- сессия должна относиться к тому же преподавателю
- сессия должна относиться к тому же предмету

Практический эффект:
- можно связать оценку с конкретным занятием
- сохраняется корректность связи между оценками и посещаемостью

### 11. Docker Compose и запуск проекта

Проект продолжает запускаться через `docker compose`.

Команда запуска:

```bash
docker compose up -d --build
```

После запуска:
- backend доступен на `http://localhost:8888`
- frontend доступен на `http://localhost:9999`
- PostgreSQL поднимается автоматически
- миграции применяются автоматически через сервис `migrate`

Практический эффект:
- новая миграция оценок применяется вместе с остальными миграциями
- backend поднимается уже с готовой структурой таблиц
- проверка сценариев не требует ручного применения SQL

### 12. Тестовые пользователи и данные

Для проверки используются тестовые пользователи:

```text
teacher_test / 123456
student_test / 123456
```

В тестовой базе есть предметы:

```text
1 - Networks
2 - Networks
```

Для `teacher_test` добавлена возможность работать с любым существующим предметом.
Это упрощает проверку backend-сценариев без ручного создания расписания.

### 13. Итог недели

За неделю был добавлен первый backend-вариант модуля оценок.

Главный результат:
- добавлена структура БД для оценок
- добавлены ограничения БРС на уровне PostgreSQL
- реализован отдельный репозиторий оценок
- репозиторий подключен к общему `Store`
- добавлена service-логика для преподавателя и студента
- добавлены HTTP endpoints для оценок
- студент получает только собственные оценки
- преподаватель может создавать контрольные точки и выставлять баллы
- оценки можно связать с attendance-сессией
- проект продолжает запускаться через `docker compose`

После этой недели в системе появился второй важный блок электронного журнала:
- ранее был реализован учет посещаемости
- теперь добавлен учет оценок и контрольных точек
