# Отчёт о проделанной работе
**Период:** 11 неделя второго сезона проекта  
**Дата составления:** 15.05.2026  

## Описание проделанной работы

За одиннадцатую неделю работы над проектом в этом сезоне были выполнены следующие шаги:
* Продумана антифрод система совместно с Вадимом
* Она же и имплементирована в Android app 
* Согласованы изменения в базе данных с Ромой
* Добавлена миграция для антифрод хендлинга
* Небольшой фикс frontend интерфейса
* Изменён workflow file для сохранения схемы базы данных 
* Сканер QR теперь открывается в вертикальной ориентации
## Поэтапное описание

### Добавлена антифрод система
Теперь с юзера на Android будут собираться 3 дополнительных параметра:
Android ID - Для того чтобы система могла понимать есть ли в базе данных уже человек с таким же устройством (чтобы нельзя было передавать аккаунты)?
Latitude, Longitude - Для отметки по QR (Человек отметился честно из аудитории или сидит дома и думает, что самый умный)
В случае если в базе данных присутствует Android ID или слишком большое расхождение по расстоянию от преподавателя (нужно считать по гаверсинусу на бекенде), то пользователь помечается для преподавателя красным цветом. Пополняется счётчик fraud'ов для этого юзера и указывается причина почему пользователь был занесён в fraud таблицу.
При накоплении какого-то критического обьема сообщений (или по таймеру) планируется отправка сообщению администратору по почте (возможно оно и не надо)

В итоге имеем такую функцию для отметки по QR:

```kotlin
override suspend fun markAttendanceViaQr(
        lessonId: Int,
        deviceId: String,
        lat: Double,
        lon: Double
    ): AttendanceResult = withContext(Dispatchers.IO) {
        try {
            val token = sharedPrefs.getString("auth_token", "") ?: ""
            if (token.isEmpty()) return@withContext AttendanceResult.Error("No token")

            val jsonRequest = JSONObject().apply {
                put("lesson_id", lessonId)
                put("device_id", deviceId)
                put("lat", lat)
                put("lon", lon)
            }

            val body = jsonRequest.toString().toRequestBody(JSON_TYPE)
            val request = Request.Builder()
                .url("$BASE_URL/api/student/mark-attendance")
                .post(body)
                .addHeader("Authorization", token)
                .build()

            val response = client.newCall(request).execute()
            val responseStr = response.body?.string() ?: ""

            if (!response.isSuccessful) {
                val errorMsg = JSONObject(responseStr).optString("error", "Error ${response.code}")
                return@withContext AttendanceResult.Error(errorMsg)
            }

            val jsonResponse = JSONObject(responseStr)
            if (jsonResponse.optBoolean("ok")) {
                val result = jsonResponse.getJSONObject("result")
                val student = Student(
                    id = result.optInt("user_ID", 0),
                    studentName = result.optString("login", ""),
                    studentGroup = result.optString("group", ""),
                    studentNFC = result.optString("nfc_tag", ""),
                    attendance = true,
                    role = result.optString("role", "student"),
                    isFraud = result.optBoolean("is_fraud", false),
                    totalCheatAttempts = result.optInt("total_cheat_attempts", 0)
                )
                studentDao.insertStudent(student)
                AttendanceResult.Success(student)
            } else {
                AttendanceResult.Error(jsonResponse.optString("error", "Failed"))
            }
        } catch (e: Exception) {
            AttendanceResult.Error("Network error")
        }
    }
```

### Добавлена миграция для базы данных

```sql
-- +goose Up
-- +goose StatementBegin

ALTER TABLE public.attendance_sessions 
ADD COLUMN teacher_latitude DOUBLE PRECISION,
ADD COLUMN teacher_longitude DOUBLE PRECISION;
ALTER TABLE public.attendance_session_students
ADD COLUMN is_fraud BOOLEAN DEFAULT false NOT NULL,
ADD COLUMN fraud_reason CHARACTER VARYING(50);
ALTER TABLE public.students
ADD COLUMN total_cheat_attempts INTEGER DEFAULT 0 NOT NULL;
CREATE TABLE public.student_devices (
    device_id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    student_id INTEGER NOT NULL REFERENCES public.students(student_id) ON DELETE CASCADE,
    device_fingerprint CHARACTER VARYING(255) NOT NULL,
    is_verified BOOLEAN DEFAULT true NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW() NOT NULL,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW() NOT NULL
);

CREATE UNIQUE INDEX idx_student_devices_fingerprint ON public.student_devices(device_fingerprint);
CREATE INDEX idx_student_devices_student ON public.student_devices(student_id);
CREATE TABLE public.anti_cheat_logs (
    log_id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    student_id INTEGER NOT NULL REFERENCES public.students(student_id) ON DELETE CASCADE,
    session_id INTEGER NOT NULL REFERENCES public.attendance_sessions(session_id) ON DELETE CASCADE,
    violation_type CHARACTER VARYING(50) NOT NULL, -- 'DISTANCE_EXCEEDED' или 'DEVICE_MISMATCH' если девайс уже в базе
    student_latitude DOUBLE PRECISION,
    student_longitude DOUBLE PRECISION,
    calculated_distance DOUBLE PRECISION,
    device_fingerprint CHARACTER VARYING(255),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW() NOT NULL
);

CREATE INDEX idx_anti_cheat_logs_student ON public.anti_cheat_logs(student_id);
CREATE INDEX idx_anti_cheat_logs_created_at ON public.anti_cheat_logs(created_at);

-- +goose StatementEnd

-- +goose Down
-- +goose StatementBegin

DROP TABLE IF EXISTS public.anti_cheat_logs;
DROP TABLE IF EXISTS public.student_devices;

ALTER TABLE public.students
DROP COLUMN IF EXISTS total_cheat_attempts;

ALTER TABLE public.attendance_session_students 
DROP COLUMN IF EXISTS is_fraud,
DROP COLUMN IF EXISTS fraud_reason;

ALTER TABLE public.attendance_sessions 
DROP COLUMN IF EXISTS teacher_latitude,
DROP COLUMN IF EXISTS teacher_longitude;

-- +goose StatementEnd
```

### Изменена deploy секция в github actions workflow
Добавил бекап схемы базы данных, если затрутся какие-либо важные данные и их нужно будет восстановить
Соответственно секция деплоя была изменена следующим образом:
```yaml
  deploy:
    needs: validate
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT }}
          # Делает бекап схемы бд до + после залива кода на production server в папку backup
          script: |
            cd /home/sibsutis-ms/ejournal
            mkdir -p backups
            TIMESTAMP=$(date +'%Y%m%d_%H%M%S')
            
            docker compose exec -T postgres pg_dump -U postgres -d ejournal > ./backups/prev_${TIMESTAMP}.sql
            chmod 600 ./backups/prev_${TIMESTAMP}.sql
            
            git pull origin main
            docker compose up -d --build
            
            docker compose exec -T postgres pg_dump -U postgres -d ejournal > ./backups/new_${TIMESTAMP}.sql
            chmod 600 ./backups/new_${TIMESTAMP}.sql
            
            find ./backups/ -name "*.sql" -mtime +14 -delete
```

